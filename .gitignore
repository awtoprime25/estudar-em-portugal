# Segredos — nunca versionar
.env

# Runtime (logs, locks, rate-limit) — gerados automaticamente, não faz sentido versionar
/storage/
/log/
*.log

# Editor/OS
.DS_Store
Thumbs.db

# Template de env — mantido só localmente, a pedido
.env.example
